# 阶段 3: 工具系统深入

**难度**: ⭐⭐
**预计时间**: 3-4 天
**前置要求**: 完成阶段 1-2

---

## 🎯 学习目标

完成本阶段后，你将能够：
- ✅ 深入理解工具基类和注册机制
- ✅ 分析所有内置工具的实现
- ✅ 创建自定义工具并集成到系统
- ✅ 编写工具的测试用例
- ✅ 理解工具参数验证机制

---

## 📖 工具系统架构

### 工具基类详解

**文件**: `nanobot/agent/tools/base.py`

```python
class Tool(ABC):
    """工具抽象基类"""

    # 类型映射：JSON Schema 类型 → Python 类型
    _TYPE_MAP = {
        "string": str,
        "integer": int,
        "number": (int, float),
        "boolean": bool,
        "array": list,
        "object": dict,
    }

    @property
    @abstractmethod
    def name(self) -> str:
        """工具名称，用于 LLM 调用"""
        pass

    @property
    @abstractmethod
    def description(self) -> str:
        """工具描述，告诉 LLM 这个工具的作用"""
        pass

    @property
    @abstractmethod
    def parameters(self) -> dict[str, Any]:
        """JSON Schema 格式的参数定义"""
        pass

    @abstractmethod
    async def execute(self, **kwargs: Any) -> str:
        """执行工具逻辑"""
        pass

    def validate_params(self, params: dict[str, Any]) -> list[str]:
        """验证参数，返回错误列表"""
        pass
```

### 工具注册表

**文件**: `nanobot/agent/tools/registry.py`

```python
class ToolRegistry:
    """工具注册表"""

    def __init__(self):
        self._tools: dict[str, Tool] = {}

    def register(self, tool: Tool) -> None:
        """注册工具"""
        self._tools[tool.name] = tool

    def get(self, name: str) -> Tool | None:
        """获取工具"""
        return self._tools.get(name)

    def list_all(self) -> list[Tool]:
        """列出所有工具"""
        return list(self._tools.values())

    def to_function_schemas(self) -> list[dict]:
        """转换为 LLM 函数调用格式"""
        return [
            {
                "name": tool.name,
                "description": tool.description,
                "parameters": tool.parameters,
            }
            for tool in self._tools.values()
        ]
```

---

## 🔧 内置工具详解

### 1. 文件操作工具

**文件**: `nanobot/agent/tools/filesystem.py`

#### ReadFileTool - 读取文件

```python
class ReadFileTool(Tool):
    def __init__(self, allowed_dir: Path | None = None):
        self.allowed_dir = allowed_dir  # 沙箱限制

    @property
    def name(self) -> str:
        return "read_file"

    @property
    def description(self) -> str:
        return "Read the contents of a file"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "path": {
                    "type": "string",
                    "description": "Path to the file"
                }
            },
            "required": ["path"]
        }

    async def execute(self, **kwargs) -> str:
        path = Path(kwargs["path"])

        # 沙箱检查
        if self.allowed_dir:
            if not self._is_within_allowed_dir(path):
                return f"Error: Access denied (outside workspace)"

        # 读取文件
        try:
            content = path.read_text(encoding="utf-8")
            return f"File: {path}\n\n{content}"
        except Exception as e:
            return f"Error reading file: {e}"
```

**关键点**:
- 支持沙箱模式（`allowed_dir`）
- 错误处理返回友好消息
- 返回格式包含文件路径

#### WriteFileTool - 写入文件

```python
class WriteFileTool(Tool):
    @property
    def name(self) -> str:
        return "write_file"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "path": {"type": "string"},
                "content": {"type": "string"}
            },
            "required": ["path", "content"]
        }

    async def execute(self, **kwargs) -> str:
        path = Path(kwargs["path"])
        content = kwargs["content"]

        # 创建父目录
        path.parent.mkdir(parents=True, exist_ok=True)

        # 写入文件
        path.write_text(content, encoding="utf-8")
        return f"Successfully wrote to {path}"
```

#### EditFileTool - 编辑文件

```python
class EditFileTool(Tool):
    """查找并替换文件内容"""

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "path": {"type": "string"},
                "old_text": {"type": "string"},
                "new_text": {"type": "string"}
            },
            "required": ["path", "old_text", "new_text"]
        }

    async def execute(self, **kwargs) -> str:
        path = Path(kwargs["path"])
        old_text = kwargs["old_text"]
        new_text = kwargs["new_text"]

        content = path.read_text(encoding="utf-8")

        if old_text not in content:
            return f"Error: Text not found in {path}"

        new_content = content.replace(old_text, new_text)
        path.write_text(new_content, encoding="utf-8")

        return f"Successfully edited {path}"
```

#### ListDirTool - 列出目录

```python
class ListDirTool(Tool):
    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "path": {
                    "type": "string",
                    "description": "Directory path (default: current)"
                }
            }
        }

    async def execute(self, **kwargs) -> str:
        path = Path(kwargs.get("path", "."))

        items = []
        for item in sorted(path.iterdir()):
            prefix = "📁" if item.is_dir() else "📄"
            items.append(f"{prefix} {item.name}")

        return f"Contents of {path}:\n" + "\n".join(items)
```

---

### 2. Shell 执行工具

**文件**: `nanobot/agent/tools/shell.py`

```python
class ExecTool(Tool):
    def __init__(
        self,
        working_dir: str,
        timeout: int = 30,
        restrict_to_workspace: bool = False
    ):
        self.working_dir = working_dir
        self.timeout = timeout
        self.restrict_to_workspace = restrict_to_workspace

    @property
    def name(self) -> str:
        return "exec"

    @property
    def description(self) -> str:
        return "Execute a shell command"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "command": {
                    "type": "string",
                    "description": "Shell command to execute"
                }
            },
            "required": ["command"]
        }

    async def execute(self, **kwargs) -> str:
        command = kwargs["command"]

        # 安全检查
        if self.restrict_to_workspace:
            if self._is_dangerous_command(command):
                return "Error: Command blocked by security policy"

        # 执行命令
        try:
            process = await asyncio.create_subprocess_shell(
                command,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
                cwd=self.working_dir
            )

            stdout, stderr = await asyncio.wait_for(
                process.communicate(),
                timeout=self.timeout
            )

            output = stdout.decode() + stderr.decode()
            return f"Exit code: {process.returncode}\n\n{output}"

        except asyncio.TimeoutError:
            return f"Error: Command timed out after {self.timeout}s"
        except Exception as e:
            return f"Error executing command: {e}"

    def _is_dangerous_command(self, command: str) -> bool:
        """检查危险命令"""
        dangerous = ["rm -rf", "dd if=", "mkfs", "> /dev/"]
        return any(d in command for d in dangerous)
```

**关键点**:
- 异步执行（`asyncio.create_subprocess_shell`）
- 超时控制
- 安全检查（沙箱模式）
- 返回退出码和输出

---

### 3. Web 工具

**文件**: `nanobot/agent/tools/web.py`

#### WebSearchTool - 网页搜索

```python
class WebSearchTool(Tool):
    def __init__(self, api_key: str | None = None):
        self.api_key = api_key  # Brave Search API

    @property
    def name(self) -> str:
        return "web_search"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "Search query"
                },
                "count": {
                    "type": "integer",
                    "description": "Number of results (default: 5)",
                    "minimum": 1,
                    "maximum": 20
                }
            },
            "required": ["query"]
        }

    async def execute(self, **kwargs) -> str:
        if not self.api_key:
            return "Error: Brave Search API key not configured"

        query = kwargs["query"]
        count = kwargs.get("count", 5)

        # 调用 Brave Search API
        url = "https://api.search.brave.com/res/v1/web/search"
        headers = {"X-Subscription-Token": self.api_key}
        params = {"q": query, "count": count}

        async with httpx.AsyncClient() as client:
            response = await client.get(url, headers=headers, params=params)
            data = response.json()

        # 格式化结果
        results = []
        for item in data.get("web", {}).get("results", []):
            results.append(
                f"**{item['title']}**\n"
                f"{item['url']}\n"
                f"{item.get('description', '')}\n"
            )

        return "\n".join(results) or "No results found"
```

#### WebFetchTool - 抓取网页

```python
class WebFetchTool(Tool):
    @property
    def name(self) -> str:
        return "web_fetch"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "url": {
                    "type": "string",
                    "description": "URL to fetch"
                }
            },
            "required": ["url"]
        }

    async def execute(self, **kwargs) -> str:
        url = kwargs["url"]

        async with httpx.AsyncClient() as client:
            response = await client.get(url, follow_redirects=True)
            html = response.text

        # 使用 readability 提取主要内容
        from readability import Document
        doc = Document(html)

        title = doc.title()
        content = doc.summary()

        # 转换为纯文本
        from html import unescape
        import re
        text = re.sub(r'<[^>]+>', '', content)
        text = unescape(text)

        return f"**{title}**\n\n{text[:5000]}"  # 限制长度
```

---

### 4. 消息工具

**文件**: `nanobot/agent/tools/message.py`

```python
class MessageTool(Tool):
    """发送消息到频道"""

    def __init__(self, send_callback):
        self.send_callback = send_callback

    @property
    def name(self) -> str:
        return "send_message"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "channel": {"type": "string"},
                "chat_id": {"type": "string"},
                "content": {"type": "string"}
            },
            "required": ["channel", "chat_id", "content"]
        }

    async def execute(self, **kwargs) -> str:
        message = OutboundMessage(
            channel=kwargs["channel"],
            chat_id=kwargs["chat_id"],
            content=kwargs["content"]
        )

        await self.send_callback(message)
        return "Message sent successfully"
```

---

### 5. 子代理工具

**文件**: `nanobot/agent/tools/spawn.py`

```python
class SpawnTool(Tool):
    """创建子代理执行后台任务"""

    def __init__(self, subagent_manager):
        self.manager = subagent_manager

    @property
    def name(self) -> str:
        return "spawn"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "task": {
                    "type": "string",
                    "description": "Task description for subagent"
                },
                "background": {
                    "type": "boolean",
                    "description": "Run in background (default: false)"
                }
            },
            "required": ["task"]
        }

    async def execute(self, **kwargs) -> str:
        task = kwargs["task"]
        background = kwargs.get("background", False)

        subagent_id = await self.manager.spawn(task, background)

        if background:
            return f"Subagent {subagent_id} started in background"
        else:
            result = await self.manager.wait(subagent_id)
            return f"Subagent completed:\n{result}"
```

---

### 6. 定时任务工具

**文件**: `nanobot/agent/tools/cron.py`

```python
class CronTool(Tool):
    """管理定时任务"""

    def __init__(self, cron_service):
        self.service = cron_service

    @property
    def name(self) -> str:
        return "cron"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "action": {
                    "type": "string",
                    "enum": ["add", "list", "remove"]
                },
                "name": {"type": "string"},
                "message": {"type": "string"},
                "cron": {"type": "string"},  # Cron 表达式
                "every": {"type": "integer"}  # 间隔秒数
            },
            "required": ["action"]
        }

    async def execute(self, **kwargs) -> str:
        action = kwargs["action"]

        if action == "add":
            job_id = await self.service.add_job(
                name=kwargs["name"],
                message=kwargs["message"],
                cron=kwargs.get("cron"),
                every=kwargs.get("every")
            )
            return f"Job {job_id} added successfully"

        elif action == "list":
            jobs = await self.service.list_jobs()
            return "\n".join(f"- {j['name']}: {j['schedule']}" for j in jobs)

        elif action == "remove":
            await self.service.remove_job(kwargs["name"])
            return f"Job {kwargs['name']} removed"
```

---

## 🛠️ 创建自定义工具

### 示例: 天气查询工具

```python
# nanobot/agent/tools/weather.py
import httpx
from typing import Any
from nanobot.agent.tools.base import Tool

class WeatherTool(Tool):
    """查询天气信息"""

    def __init__(self, api_key: str):
        self.api_key = api_key

    @property
    def name(self) -> str:
        return "get_weather"

    @property
    def description(self) -> str:
        return "Get current weather for a city"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "city": {
                    "type": "string",
                    "description": "City name (e.g., 'Beijing', 'New York')"
                },
                "units": {
                    "type": "string",
                    "enum": ["metric", "imperial"],
                    "description": "Temperature units (default: metric)"
                }
            },
            "required": ["city"]
        }

    async def execute(self, **kwargs: Any) -> str:
        city = kwargs["city"]
        units = kwargs.get("units", "metric")

        # 调用天气 API (示例使用 OpenWeatherMap)
        url = "https://api.openweathermap.org/data/2.5/weather"
        params = {
            "q": city,
            "appid": self.api_key,
            "units": units
        }

        try:
            async with httpx.AsyncClient() as client:
                response = await client.get(url, params=params)
                response.raise_for_status()
                data = response.json()

            # 提取信息
            temp = data["main"]["temp"]
            feels_like = data["main"]["feels_like"]
            description = data["weather"][0]["description"]
            humidity = data["main"]["humidity"]

            unit_symbol = "°C" if units == "metric" else "°F"

            return (
                f"Weather in {city}:\n"
                f"Temperature: {temp}{unit_symbol}\n"
                f"Feels like: {feels_like}{unit_symbol}\n"
                f"Condition: {description}\n"
                f"Humidity: {humidity}%"
            )

        except httpx.HTTPStatusError as e:
            return f"Error: Unable to fetch weather data ({e.response.status_code})"
        except Exception as e:
            return f"Error: {str(e)}"
```

### 注册自定义工具

```python
# nanobot/agent/loop.py
def _register_default_tools(self):
    # ... 其他工具

    # 注册天气工具
    from nanobot.agent.tools.weather import WeatherTool
    weather_api_key = os.getenv("OPENWEATHER_API_KEY")
    if weather_api_key:
        self.tools.register(WeatherTool(weather_api_key))
```

---

## 🧪 工具测试

### 测试框架

```python
# tests/test_weather_tool.py
import pytest
from nanobot.agent.tools.weather import WeatherTool

@pytest.fixture
def weather_tool():
    return WeatherTool(api_key="test_key")

def test_tool_name(weather_tool):
    assert weather_tool.name == "get_weather"

def test_tool_description(weather_tool):
    assert "weather" in weather_tool.description.lower()

def test_parameters_schema(weather_tool):
    params = weather_tool.parameters
    assert params["type"] == "object"
    assert "city" in params["properties"]
    assert "city" in params["required"]

def test_validate_params_valid(weather_tool):
    errors = weather_tool.validate_params({"city": "Beijing"})
    assert len(errors) == 0

def test_validate_params_missing_required(weather_tool):
    errors = weather_tool.validate_params({})
    assert any("missing required city" in e for e in errors)

def test_validate_params_invalid_enum(weather_tool):
    errors = weather_tool.validate_params({
        "city": "Beijing",
        "units": "invalid"
    })
    assert any("must be one of" in e for e in errors)

@pytest.mark.asyncio
async def test_execute_success(weather_tool, httpx_mock):
    # Mock API 响应
    httpx_mock.add_response(
        url="https://api.openweathermap.org/data/2.5/weather",
        json={
            "main": {"temp": 20, "feels_like": 18, "humidity": 60},
            "weather": [{"description": "clear sky"}]
        }
    )

    result = await weather_tool.execute(city="Beijing")
    assert "Beijing" in result
    assert "20" in result
    assert "clear sky" in result

@pytest.mark.asyncio
async def test_execute_api_error(weather_tool, httpx_mock):
    # Mock API 错误
    httpx_mock.add_response(
        url="https://api.openweathermap.org/data/2.5/weather",
        status_code=404
    )

    result = await weather_tool.execute(city="InvalidCity")
    assert "Error" in result
```

### 运行测试

```bash
# 运行所有测试
pytest tests/test_weather_tool.py

# 运行特定测试
pytest tests/test_weather_tool.py::test_execute_success

# 显示详细输出
pytest tests/test_weather_tool.py -v

# 显示打印输出
pytest tests/test_weather_tool.py -s
```

---

## 📝 实践任务

### 任务 1: 分析内置工具

选择 3 个内置工具，分析其实现：

1. **ReadFileTool**
   - 如何实现沙箱限制？
   - 错误处理策略是什么？

2. **ExecTool**
   - 如何实现超时控制？
   - 安全检查包含哪些内容？

3. **WebSearchTool**
   - API 调用流程是什么？
   - 结果如何格式化？

### 任务 2: 创建计算器工具

创建一个安全的计算器工具：

```python
class CalculatorTool(Tool):
    """安全的数学计算工具"""

    @property
    def name(self) -> str:
        return "calculate"

    @property
    def description(self) -> str:
        return "Evaluate a mathematical expression safely"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "expression": {
                    "type": "string",
                    "description": "Math expression (e.g., '2 + 2', '10 * 5')"
                }
            },
            "required": ["expression"]
        }

    async def execute(self, **kwargs) -> str:
        expression = kwargs["expression"]

        # TODO: 实现安全的表达式求值
        # 提示: 使用 ast.literal_eval 或 numexpr
        pass
```

**要求**:
- 支持基本运算（+、-、*、/、**）
- 防止代码注入
- 处理除零错误
- 编写测试用例

### 任务 3: 创建 JSON 工具

创建一个 JSON 处理工具：

```python
class JsonTool(Tool):
    """JSON 数据处理工具"""

    @property
    def name(self) -> str:
        return "json_process"

    @property
    def parameters(self) -> dict[str, Any]:
        return {
            "type": "object",
            "properties": {
                "action": {
                    "type": "string",
                    "enum": ["parse", "format", "query"]
                },
                "data": {"type": "string"},
                "query": {"type": "string"}  # JSONPath 查询
            },
            "required": ["action", "data"]
        }

    async def execute(self, **kwargs) -> str:
        # TODO: 实现 JSON 解析、格式化、查询功能
        pass
```

**功能**:
- `parse`: 验证 JSON 格式
- `format`: 美化 JSON
- `query`: 使用 JSONPath 查询

### 任务 4: 编写完整测试

为你创建的工具编写完整的测试套件：

```python
# tests/test_calculator_tool.py

def test_basic_operations():
    """测试基本运算"""
    pass

def test_complex_expressions():
    """测试复杂表达式"""
    pass

def test_error_handling():
    """测试错误处理"""
    pass

def test_security():
    """测试安全性（防止代码注入）"""
    pass
```

---

## ✅ 阶段检验

- [ ] 理解工具基类的设计
- [ ] 分析至少 3 个内置工具的实现
- [ ] 创建至少 2 个自定义工具
- [ ] 编写工具的测试用例
- [ ] 理解参数验证机制
- [ ] 掌握工具注册流程

**自测题**:
1. 工具的 `execute` 方法为什么是异步的？
2. JSON Schema 在工具中的作用是什么？
3. 如何实现工具的沙箱限制？
4. 工具测试应该覆盖哪些方面？

---

## 📚 扩展阅读

- [agent/tools/base.py](../nanobot/agent/tools/base.py)
- [agent/tools/filesystem.py](../nanobot/agent/tools/filesystem.py)
- [JSON Schema 规范](https://json-schema.org/)
- [pytest 文档](https://docs.pytest.org/)

---

## 🎉 恭喜！

你已经完成了第三阶段的学习！现在你：
- ✅ 深入理解了工具系统
- ✅ 能够创建自定义工具
- ✅ 掌握了工具测试方法

准备好了吗？让我们进入 [阶段 4: 技能系统实践](./04-技能系统实践.md)！
